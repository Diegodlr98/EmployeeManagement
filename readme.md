# EmployeeManagementProject

This is a small project to show some work using a cap template based on json, xsuaa security and hana as the database, the idea is also to get the small sample data with postman to show up that i can work with it too using a xsuaa authtentication

the idea of the project is to simulate an app that storages employees data such as theyr id, email, name etc. and theyr departments, and to be available to search up or get that information
